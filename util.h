#ifndef UTIL_H
#define UTIL_H

#include <vector>
#include <queue>
#include <set>
#include <string>
using namespace std;
namespace sol1181
{
    class Util
    {
    public:
        static vector<string> split(string input, const char *delimiter);
    };
}
#endif